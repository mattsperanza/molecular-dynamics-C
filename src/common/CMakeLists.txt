set(CURRENT_DIR src/common/)
set(
        COMMON
        # Paths from root
        # numerics/
        # parsers/
        # script/
        ${CURRENT_DIR}scripts/commandInterpreter.c
        # utils/
        ## utils/ds
        ${CURRENT_DIR}utils/ds/list.c
        PARENT_SCOPE
)
